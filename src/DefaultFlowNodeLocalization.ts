import { FlowNodeLocalization } from ".";

/** @public */
export const DefaultFlowNodeLocalization: Readonly<FlowNodeLocalization> = Object.freeze({
    holdCtrlKeyToEnableLink: "Hold CTRL key to enable link",
});
